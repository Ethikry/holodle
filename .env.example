# Copy to .env and fill in values. .env is gitignored.

# --- Discord application ---
# From https://discord.com/developers/applications -> your app
# OAuth2 -> Client information
DISCORD_CLIENT_ID=
# OAuth2 -> Client information -> Reset Secret (server-only — never exposed to client)
DISCORD_CLIENT_SECRET=
# General Information -> Public Key. Used to verify Ed25519 signatures on
# incoming /api/interactions requests. Set this before saving the
# Interactions Endpoint URL in the dev portal — Discord pings the endpoint
# on save and won't accept it until PING is signed and verified correctly.
DISCORD_PUBLIC_KEY=

# --- Server ---
# Port the Fastify server listens on. The Vite dev server proxies /api here.
PORT=3001
# Path to the SQLite database file. In Docker/Fly this is /data/holodle.db.
DB_PATH=./holodle.db
# Comma-separated origins allowed by CORS in dev. Discord proxies the activity
# through https://<client_id>.discordsays.com so production needs no CORS.
CORS_ORIGINS=http://localhost:5173
# Admin token for accessing /api/admin/stats (aggregated usage analytics).
# If not set, the endpoint is disabled. Use a strong random token in production.
ADMIN_TOKEN=

# --- Client (Vite) ---
# Exposed to the browser bundle. The client_id is public; the secret is NOT.
VITE_DISCORD_CLIENT_ID=
